rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Express backend server proxies all database requests with server-side RBAC and validation
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
